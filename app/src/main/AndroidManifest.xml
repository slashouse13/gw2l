<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.scarleshbrihouse.slashouse.gw2tool">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        tools:ignore="HardcodedDebugMode">
        <activity
            android:name=".MainMenu"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TodayDailies"
            android:label="@string/title_activity_today_dailies" />
        <activity
            android:name=".Events"
            android:label="@string/title_activity_events" />
        <activity
            android:name=".MapEvents"
            android:label="@string/title_activity_map_events" />

        <meta-data
            android:name="android.support.VERSION"
            android:value="26.0.0"
            tools:replace="android:value" />

        <activity
            android:name=".pofEvents"
            android:label="@string/title_activity_pof_events"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>